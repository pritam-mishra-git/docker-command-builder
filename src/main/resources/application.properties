spring.application.name=docker-command-builder
# Render (and most PaaS hosts) inject a PORT env var at runtime; falls back to 8080 locally.
server.port=${PORT:8080}

# Keep static resources fresh during local development
spring.web.resources.cache.period=0
